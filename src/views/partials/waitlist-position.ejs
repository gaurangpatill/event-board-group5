<% if (position === null) { %>
  <span class="text-sm text-slate-500">You are not currently waitlisted.</span>
<% } else { %>
  <span class="text-sm text-slate-600">Waitlist position: <strong><%= position %></strong></span>
<% } %>
