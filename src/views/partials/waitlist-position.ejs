<% if (position !== null) { %>
  <div
    class="inline-flex items-center gap-1.5 rounded-full border border-yellow-300 bg-yellow-50 px-2.5 py-1 text-xs font-semibold text-yellow-900 ring-1 ring-yellow-200"
  >
    <span class="uppercase tracking-wide text-yellow-700">Waitlist</span>
    <span class="text-yellow-900">#<%= position %></span>
  </div>
<% } %>
